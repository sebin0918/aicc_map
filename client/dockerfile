# Node.js 이미지 사용 (build 단계)
FROM node:18 as build

# 작업 디렉토리 설정
WORKDIR /app

# 패키지 파일 복사 및 의존성 설치
COPY package*.json ./
RUN npm install  # package.json에 정의된 모든 의존성 설치

# 소스 코드 복사
COPY . .

# 환경설정 파일 복사 (.env 파일이 있을 경우)
COPY .env .env

# 빌드 실행 (프로덕션용)
RUN npm run build

# Nginx를 사용해 정적 파일 서빙
FROM nginx:alpine

# Nginx 설정 파일 복사 (경로 확인 필요)
COPY ./nginx.conf /etc/nginx/nginx.conf

# React 빌드 파일 복사
COPY --from=build /app/build /usr/share/nginx/html

# Nginx 포트 열기
EXPOSE 80

# Nginx 실행
CMD ["nginx", "-g", "daemon off;"]
